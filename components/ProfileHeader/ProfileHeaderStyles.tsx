import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        width: "100%",
    },
    name: {
        fontSize: 20,
        marginTop: 12,
        fontWeight: "500",
        textAlign: "center"
    }
});

export default styles;